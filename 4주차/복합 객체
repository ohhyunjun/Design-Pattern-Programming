1. 복합객체
  = 한 객체가 다른 객체를 포함하는 객체를 말한다. : 포함관계(has-a)
  - Whole 객체 = 전체 객체 = 복합객체 : 작은 객체들을 포함하는 큰 객체
  - Part 객체 = 부분 객체

  - 합성 객체(Composition): 부분객체가 필수적으로 있어야만 가능한 복합객체. (ex: 사람 <- 몸, 머리)
  - 집합 객체(Aggregation): 부분객체를 선택적으로 포함하는 복합객체. (ex: 버스운전사 <- 옷, 모자)
